{
  "name": "backbone",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "prisma": "^6.19.2"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": [
      "prettier --write"
    ],
    "backend/src/**/*.ts": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  }
}
